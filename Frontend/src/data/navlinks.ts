import type { INavLink } from "../types";

export const navlinks: INavLink[] = [
    { name: "Early Access", href: "#contact" },
    { name: "What It Does", href: "#features" },
    { name: "Program", href: "#testimonials" },
];